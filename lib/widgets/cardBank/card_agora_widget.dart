import 'package:flutter/material.dart';

class CardAgora extends StatelessWidget {
  const CardAgora({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: double.infinity,
      width: double.infinity,
      color: const Color.fromRGBO(0, 205, 252, 1),
    );
  }
}
